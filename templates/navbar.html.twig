{% block navbar %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="/">SortThosePhotos</a>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-item nav-link" href="{{ path('event') }}">Events</a>
            </div>

        </div>
        <div class="my-2 my-lg-0">
            {% if is_granted('ROLE_USER') %}
                <a href="/settings"><button class="btn btn-primary">Settings</button></a>
                <a href="/logout"><button class="btn btn-primary" type="button">Logout</button></a>
            {% else %}
                <a href="/login"><button class="btn btn-primary" type="button">Login</button></a>
            {% endif %}
        </div>
    </nav>
{% endblock navbar %}
